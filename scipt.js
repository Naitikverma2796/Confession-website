function openPopup() {
      window.open(
        'https://example.com',       // URL to open
        'popupWindow',               // Window name (optional)
        'width=400,height=300,left=100,top=100'  // Window features
      );
    }